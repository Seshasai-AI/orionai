# OrionAI — Python dependencies
# Pinned for Render.com (Python 3.11)
# DO NOT use tavily-python SDK — we call Tavily REST API directly via httpx

fastapi==0.115.5
uvicorn[standard]==0.32.1
gunicorn==23.0.0
httpx==0.27.2
python-dotenv==1.0.1
pydantic==2.10.3
python-multipart==0.0.19
anyio==4.6.2
starlette==0.41.3
